meta {
  name: List Users
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/api/v1/users
  body: none
  auth: bearer
}

params:query {
  ~q: search
  ~per_page: 20
}

headers {
  Accept: application/json
}

auth:bearer {
  token: {{token}}
}
