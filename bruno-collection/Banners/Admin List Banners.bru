meta {
  name: Admin List Banners
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/api/v1/admin/banners
  body: none
  auth: bearer
}

params:query {
  ~q: search
  ~filter[is_active]: true
}

headers {
  Accept: application/json
}

auth:bearer {
  token: {{token}}
}
